# The following import targets are created in NEM2:: namespace:
#
#  nemcpp
#
# This module also sets the following variables in your project:
#
#   nem2-sdk-cpp_FOUND
#   nem2-sdk-cpp_INCLUDE_DIR
#   nem2-sdk-cpp_LIBRARY

@PACKAGE_INIT@

if (NOT TARGET nemcpp)
	include("${CMAKE_CURRENT_LIST_DIR}/@package_config_prefix@-targets.cmake")
	
	if (TARGET NEM2::nemcpp)
		get_target_property(@PROJECT_NAME@_INCLUDE_DIR NEM2::nemcpp INTERFACE_INCLUDE_DIRECTORIES)
		get_target_property(@PROJECT_NAME@_LIBRARY NEM2::nemcpp LOCATION)
	endif()
endif()
